# 항구 최적화 연구 환경 의존성
# 생성일: 2026-03-19
# 목적: 재현성 보장을 위한 버전 고정

# ── 최적화 솔버 ──────────────────────────────
pyomo==6.8.0           # MILP/MINLP 모델링 프레임워크 (권장 스택 Option A)
highspy==1.9.0         # HiGHS MILP 솔버 (무료, Tier 1 기본)
ortools==9.11.4210     # Google OR-Tools (CP-SAT, VRP 참조 비교용)
gurobipy==11.0.3       # Gurobi Python API (학술 라이선스 필요, 선택적)
# pyscipopt==5.1.0     # SCIP (Pyomo 백업 솔버, 필요 시 활성화)
# cplex==22.1.1        # IBM CPLEX (유료, 선택적)

# ── 수치 계산 ─────────────────────────────────
numpy==1.26.4
scipy==1.15.1          # 통계 분포, 최적화 서브루틴
pandas==2.2.1          # 데이터 처리

# ── 확률적 최적화 ─────────────────────────────
scikit-learn==1.4.1    # GaussianMixture (GMM), KDE 지원

# ── 시각화 ───────────────────────────────────
matplotlib==3.8.3

# ── 논문 다운로드 ─────────────────────────────
arxiv==2.1.3           # arXiv API 클라이언트
requests==2.32.3       # HTTP 클라이언트 (Unpaywall API)

# ── 개발 도구 ─────────────────────────────────
pytest==8.1.1
jupyter==1.0.0
